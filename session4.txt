while getopts ":a:d:f:" opt; do
  case $opt in
    a) action=$OPTARG
		ARGS="$ARGS -a $action" ;
    ;;
    f) 
        ARGS="$ARGS -f $OPTARGS" ;
    ;;
    d) 
       ARGS="$ARGS -d $OPTARGS" ;
    ;;

    \?) echo "Invalid option -$OPTARG" >&2
    ;;
  esac
done

echo -e "THE ARGS IS $ARGS ";
